name: load_url_image
description: A new Flutter project.

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=2.18.0 <3.0.0"

dependencies:
  audioplayers: ^5.2.1
  cached_network_image: ^3.2.2
  camera: ^0.10.5+7
  camera_windows: ^0.2.1+8
  cupertino_icons: ^1.0.2
  flutter:
    sdk: flutter
  flutter_cache_manager: ^3.3.0
  image_picker: ^1.0.5
  image_picker_windows: ^0.2.1+1
  provider: ^6.1.1

dev_dependencies:
  flutter_lints: ^3.0.1
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

  assets:
    - assets/
    - assets/blocks.gif
    - assets/image-base64.txt
